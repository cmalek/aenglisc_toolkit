# ADR NNNN: <short, decision-oriented title>

Status: Proposed | Accepted | Superseded by NNNN | Deprecated
Date: YYYY-MM-DD

## Context

What problem or forces are at play? What constraints make this decision
necessary right now? State facts, not the decision itself.

## Decision

What did we choose? Be concrete — this is the part a future reader actually
needs. If the ADR bundles several related decisions, use subsections
(`### D1 — ...`, `### D2 — ...`) rather than one undifferentiated block.

## Consequences

What follows from this choice — good and bad? What does it rule out? What
follow-up work does it create?

## Alternatives considered

(Optional.) What else was on the table, and why was it rejected? Skip this
section if there was no real alternative worth naming.

## Related

(Optional.) Links to ADRs this supersedes/depends on, specs, or implementation
plans.

---

This file is a copy-paste starting point for new ADRs, not a published doc
page. It lives outside `doc/source/` so Sphinx never renders it. Copy its
contents into a new `doc/source/architecture/adr_NNNN_slug.rst` file (RST,
not Markdown — mirror the structure above using RST headings), fill it in,
and add it to the `architecture/index.rst` toctree. See AGENTS.md for the
full instructions.
